# URL of the deployed backend API
# For local development the default is http://localhost:8000 (no change needed)
# Set this to your Render/Railway/etc. backend URL for production
NEXT_PUBLIC_API_URL=http://localhost:8000
